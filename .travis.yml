language: java

before_install :
  - wget https://chromedriver.storage.googleapis.com/2.30/chromedriver_linux64.zip -O / tmp / chromedriver 
  - chmod 777 / tmp / chromedriver
env :
  - CHROME_DRIVER_PATH = / tmp / 
  
script: mvn test
